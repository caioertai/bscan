%h1 Teste
%ul
  - product = GrabService.search_by_ean(params[:ean])
  %li
    %h3= product[:name]
    %h4= product[:price].format
    %ul
      - if product[:composition]
        - product[:composition].each do |e|
          %li= e

  -# = GrabService.search_by_name('Creme de Pentear Garnier Fructis Cachos Poderosos').each do |product|
  -#   %li
  -#     %h3= product[:name]
  -#     %ul
  -#       - if product[:composition]
  -#         - product[:composition].each do |e|
  -#           %li= e
